export type Sex = 'male' | 'female' | '';

export interface IForm {
  phone: string;
  name: string;
  surname: string;
  sex: string;
  address: string;
  workplace: string;
  amount: number;
  term: number;
}
